<Window x:Class="ql_nhanSW.Form.LoadingOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ql_nhanSW.Form"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Xác thực hệ thống" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True" 
        WindowStyle="None"
        Background="Transparent">

    <Grid x:Name="MainLoadingGrid" Visibility="Visible">

        <Rectangle Fill="#0A0010" Opacity="0.9" RadiusX="16" RadiusY="16">
            <Rectangle.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="0" Color="#7C3AED" Opacity="0.5"/>
            </Rectangle.Effect>
        </Rectangle>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="538">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                         IsIndeterminate="True"
                         Width="70" Height="70"
                         Foreground="#A855F7"
                         Margin="0,0,0,20"/>

            <TextBlock Text="ĐANG XÁC THỰC QUYỀN TRUY CẬP"
                       Foreground="#E9D5FF" FontSize="18" FontWeight="Bold"
                       FontFamily="Segoe UI" Margin="0,0,0,10"
                       TextAlignment="Center"/>

            <TextBlock Text="Tài khoản của bạn hiện chưa được cấp vai trò hoặc đang chờ quản trị viên phê duyệt.&#x0a;Vui lòng liên hệ bộ phận IT hoặc thử lại sau."
                       Foreground="#9D78CC" FontSize="13"
                       FontFamily="Segoe UI" TextAlignment="Center"
                       TextWrapping="Wrap" MaxWidth="450" Margin="0,0,0,30" Width="532"/>

            <Button Content="QUAY LẠI ĐĂNG NHẬP"
                    Click="LogoutBtn_Click"
                    Width="220" Padding="0,12"
                    Background="Transparent"
                    BorderBrush="#7C3AED" BorderThickness="1"
                    Cursor="Hand"
                    Foreground="White" Height="48">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1A0035"/>
                                <Setter Property="BorderBrush" Value="#A855F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>